# Desenvolvimento local (padrão — proxy do Vite)
# VITE_API_URL=   ← deixe vazio em dev, o proxy cuida disso

# Produção — preencha com o IP ou domínio da VPS
VITE_API_URL=https://SEU_IP_OU_DOMINIO/api
VITE_API_URL_NATIVE=https://SEU_IP_OU_DOMINIO/api
